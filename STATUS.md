# Galaxy Angel - Eternal Lovers (PS2) 한글화 진행 상태

## 2026-09-08 v0.1 재배포 완료 — 5950 정식 PSS

실기에서 정상 재생이 확인된 `ga_ml_5950` 조건을 전체 자막 영상에 적용해 PSS/ISO와 v0.1 배포 파일을 다시 만들었다.

- 자막 타임라인: 39개 ASS 중 실제 자막 24편 / Dialogue 113개를 +1.00초 보정 전 상태로 원상복구한 것을 사용했다.
- 정식 영상 인코딩: MPEG-2 `b:v=5950k`, `minrate=5950k`, `maxrate=6000k`, B-frame 2, VBV `1,835,008` bytes, 원본 프레임레이트 유지.
- mux는 `ga_ml_5950` 실기 정상판과 동일하게 Galaxy Angel/Moonlit 기본 `mux()` 경로를 사용했고 PS2STR/원본-rate template/추가 prefetch·lead 보정은 사용하지 않았다.
- 자막 PSS 24편 재생성 및 picture count/mux 검증: **24/24 PASS**.
- 빈 ASS 15편은 v18 ISO의 기존 PSS를 그대로 유지했고 readback **15/15 PASS**.
- ISO 내부 교체 PSS readback: **24/24 PASS**.
- 새 PSS 용량 증가로 `GADAT101`, `103`, `110`, `114`, `120`, `121`, `137` 7편을 ISO 끝으로 재배치하고 ISO9660 LBA/size를 갱신했다.
- UI 누락 수정도 최종 ISO에 다시 적용했다: ADV FSTS 417개 동기화 및 2,039 리소스 strict 검증, `GAEL.DAT` Pause 이미지 17/17 패치·검증, 저장/불러오기 ELF 문자열 3개 패치.
- 최종 ISO: `build/Galaxy_Angel_Eternal_Lovers_KO_v0.1_RELEASE.iso`
  - 크기: `5,182,930,944 bytes`
  - MD5: `eb915b1fc2b278a8575e236954dbbdf1`
  - SHA-1: `df8e3ac6fd0db34bfa4e209fbe83cae37460ffaf`
  - SHA-256: `91e131cb5bd0178ac75eee1bd77e935f7a5f28d7deaca0506e7dc075979fd7d3`
- v0.1 XDelta: `release/galaxy_angel_eternal_lovers_ps2_kr_v0.1.xdelta`
  - 크기: `843,088,039 bytes`
  - SHA-256: `1b9b435b162c7055d3fbdb1cacffa18dbe5f5e0ad5e3745cc22dd8d7cf438074`
- 일본판 원본 ISO에 새 v0.1 XDelta를 실제 적용해 위 최종 ISO와 SHA-256 완전 일치: **PASS**.
- `release/README.txt`, `release/SHA256SUMS.txt`, `release/release.json`도 새 값으로 갱신했다.

## 2026-09-08 영상 자막 +1.00초 보정 취소

후속 실기/구조 분석 결과, 이전에 자막 싱크 문제로 판단해 적용했던 **+1.00초 이동은 취소**한다. 32~34초 부근 정지/스킵 문제는 자막 타임라인 자체가 아니라 5.95Mbps 인코딩 및 PSS/PS2STR 호환성 쪽으로 좁혀졌다.

- `movie/subtitles/GADAT100~138.ko.ass` 39개 중 실제 `Dialogue:`가 있는 **24편 / 113개 이벤트**의 시작·종료 시간을 모두 **-1.00초** 원상복구했다.
- 0초보다 앞설 수 있는 이벤트는 `0:00:00.00`으로 유지했으며, 음수 타임코드는 **0개**다.
- 빈 ASS 15편은 변경하지 않았다.
- 자막 문구/스타일/색상은 변경하지 않았다.
- 이후 위 `2026-09-08 v0.1 재배포 완료 — 5950 정식 PSS` 단계에서 실기 정상 `ga_ml_5950` 방식으로 PSS/ISO와 배포 파일까지 다시 생성했다.

## 2026-09-07 영상 자막 +1.00초 보정 및 최종 ISO 반영

실기 기준으로 영상 자막이 약 1초 빠르게 보이는 문제를 Moonlit Lovers와 같은 방식으로
보정했다. `movie/subtitles/GADAT100~138.ko.ass` 39개 중 실제 `Dialogue:`가 있는 24편,
총 113개 이벤트의 시작/종료 시간을 모두 **+1.00초** 이동했다. 빈 ASS 15편은 그대로 유지했다.

자막이 있는 24편만 다시 MPEG-2/PSS로 만들었다. Eternal Lovers의 원본 영상은 평균 약
3.85~4.10Mbps이므로 Moonlit용 5.95Mbps 고정값을 그대로 쓰면 ISO가 5.13GB까지 커졌다.
최종본은 각 원본 M2V의 실제 평균 비트레이트를 목표값으로 사용해 원본 화질·용량 설계에
맞췄고, 24편 모두 새 PSS가 기존 슬롯보다 작아져 **기존 LBA를 전부 보존**했다. ISO9660의
파일 size만 실제 새 PSS 크기로 갱신했다.

- PSS 빌드: 24/24 PASS
  - 소스/인코드 picture count 일치
  - video elementary stream readback exact
  - PCM SS stream readback exact
  - 16KiB pack alignment exact
- ISO 내부 교체 PSS readback: 24/24 SHA-256 PASS
- 빈 ASS 15편: v18 ISO의 기존 PSS와 byte-identical 15/15 PASS
- PSS 재배치: 0편, 기존 LBA 24/24 유지
- 최종 ISO 크기: `4,674,766,848` bytes (v18과 동일)
- 최종 ISO: `build/Galaxy_Angel_Eternal_Lovers_KO_v0.1_SUBTITLED.iso`
  - MD5 `8f51cc5507304044a7c87c7059f10abb`
  - SHA-1 `7e341177885adb644922f9985cd375cddfa07c8e`
  - SHA-256 `45445c0a71d6cca9b04db034157b88a9ab1b7a878698da8ae9d688360ad29700`
- 검증 보고서:
  - `build/eternal_lovers_subtitled_pss_report.json`
  - `build/eternal_lovers_movies_iso_patch.json`

배포 `release/`의 기존 v0.1 XDelta는 아직 갱신하지 않았다. 새 XDelta는 일본판 원본 ISO
(SHA-256 `31cb2a0b6a219323ea8fc451050a75f06fc0947fb0ff33b182835adf7b6da25d`)가 필요하지만,
현재 Codex가 접근 가능한 `D:\trans\translation-assistant` 범위에는 해당 ISO가 없다.
따라서 기존 릴리즈 파일을 잘못된 대상으로 덮어쓰지 않고 그대로 보존했다.

갱신: 2026-09-04 (진행 정지 원인 규명과 v1.5 재빌드)

## 2026-09-04 초반 정지 원인과 선택지·화자·브리핑 수정 (v1.5)

사용자가 v1.4에서 **프롤로그 6번째 내레이션을 지나면 진행이 멈춘다**고 보고했다.
정지 지점 세이브스테이트의 EE 메모리를 직접 떠서 원인을 확정했다.

### 정지 원인: 완전 공백 슬롯

- 정지 지점은 `SCENARIO_DAT_00002800:isb:0006`, 415유닛 중 6번째다.
- 해당 ISB 블록은 **일본어 원본과 같은 주소 `0xE34100`** 에, 인덱스 테이블과 명령
  구조까지 바이트 단위로 동일하게 로드돼 있었다(`analysis/first_dialogue_state/
  eeMemory.bin`의 일본어판 상태와 대조). EE 스택도 얕은 메인 루프였다.
  → **컨테이너 extent 이동은 이 정지의 원인이 아니다.** 스크립트는 정상적으로 읽혔다.
- 일본어 원본은 고정 슬롯 58,320개를 **마지막 바이트까지 빈틈없이** 채운다. 빈 줄이
  존재하지 않는 포맷이라 엔진에 그 처리 경로가 없다.
- `fit_slots`가 앞 슬롯부터 그리디로 채우는 바람에 한국어가 짧으면 마지막 줄이
  통째로 반각 공백(`0xA0`)이 됐다. 게임 전체 **최초의 완전 공백 줄이 바로 다음 유닛
  `:0007`의 세 번째 줄**이었고, 전체로는 2,274곳이었다.
- `tools/eternal_lovers_patch_isb.py`의 `fit_slots`를 **슬롯 크기 비례 배분**으로
  바꿨다. 35,929유닛 / 58,320슬롯에서 완전 공백 슬롯 0, 오버플로 0이다.

### 선택지가 서로 겹치던 문제: ISB 추출기의 병합 규칙

- 추출기가 물리적으로 붙어 있는 문자열 레코드를 전부 병합했는데, STX 컴파일러는
  인자 문자열을 연달아 배치하므로 **별개의 문자열도 붙어 있다**. 그래서
  `もう一度確認する` + `その必要はない` 두 선택지가 한 문장으로 합쳐졌고, 번역문이
  두 칸에 임의로 잘려 들어갔다. 브리핑 패널(7~10줄)도 마찬가지였다.
- 문장 앞 4바이트 함수 토큰은 블록 키로 암호화되지 않은 고정 해시라 호출 종류를
  정적으로 구분할 수 있다: `084368c5` 메시지 / `9f455280` 선택지 / `af0e22af` 타이틀 /
  `a7074316` 음성.
- `tools/eternal_lovers_regroup_isb.py`를 추가해 **메시지 문장(3줄 이하)만 병합**하고
  선택지·브리핑 패널은 레코드마다 분리했다. 기존 번역 35,798개를 승계했고 갈라진
  468개만 새로 번역했다(`assets/translation/isb_split_units_ko.json`, 403건 적용;
  나머지는 복호 실패 레코드라 원문 유지).

### 미번역 브리핑: `ADV.DAT` 런타임 사본

- `ADV.DAT`는 ADV 프레임의 파일 캐시로 **SCENARIO 스크립트 30블록의 바이트 동일
  사본**과 `adv_string.tbl` 사본 3개를 갖고 있는데 아무 단계도 패치하지 않았다.
  그 30블록이 정확히 임무 브리핑 블록들이라 승리/패배 조건이 일본어로 남았다.
- `tools/eternal_lovers_patch_adv_runtime.py`를 추가했다. Moonlit의 같은 도구는
  슬롯에 안 들어가면 실패하지만 Eternal Lovers의 ADV 뱅크는 더 빡빡해서, 안 맞으면
  **FSTS 뱅크를 통째로 재패킹**한다(뱅크 시작·경계·컨테이너 크기 유지).

### 화자 이름: 추출된 적 없는 리소스

- 이름판이 일본어였던 이유는 `dat/gadat000/adv_string.tbl`이 텍스트 감사에서
  **통째로 누락**됐기 때문이다(candidates 0건). 정지 지점 EE 메모리에서 `[SPEAKER]`
  섹션을 찾아 역추적했다.
- 화자명 외에 장 타이틀 17개, SLG 스테이지명 12개, 무비명 38개가 같은 파일에 있다.
  `tools/eternal_lovers_extract_adv_string_table.py`로 88개 항목을 candidates에
  편입했다(`assets/translation/adv_string_table_ko.json`). 장 타이틀은 `gktitle*.tex`
  이미지에 이미 들어간 한국어와 문구를 통일했다.

### 빌더 정리

- `moonlit_lovers_build.py`에 1편 v42의 **고정 extent + backing 우회**를 이식했다.
  다만 이 디스크는 4.72GB라 32비트 PIDX 오프셋이 디스크 끝까지 닿지 않으므로,
  닿지 않을 때는 경고를 찍고 기존처럼 컨테이너를 이동한다(SCENARIO·SLG 해당).
  Moonlit처럼 4GiB 안에 들어오는 디스크에서는 원본 위치가 유지된다.
- `eternal_lovers_build_images.py`의 순서를 바꿨다: 원본 ISO 복사 → 이미지 → ELF →
  잔여 텍스트 → SCENARIO → **ADV 런타임 사본** → 전투 뱅크 이미지 → 검증.
- 컨테이너 뷰를 memoryview로 바꿔 논리 크기가 커진 컨테이너를 복사 없이 검증한다.

### 최종 ISO와 검증

- `build/Galaxy_Angel_Eternal_Lovers_KO_v15.iso`
  - 크기 `4,723,036,160` bytes
  - SHA-256 `e57d56393f7143b1d3b6a4d1de765311a9fee2d79f32c489cf55d3530614c639`
- ISB 삽입 36,198유닛 / 134리소스, 오버플로 0, 누락 글리프 0
- ISO 내부 ISB 134리소스 전수 일치 (`build/iso_isb_verify_v15.json`)
- 비-ISB: ADV 4 / GADAT000 3 / SCENARIO 18 / SLG 424 / SLGRES 3 / SLGSTAGE 1,258
  리소스, 590,702개 문자열 전수 일치 (`build/remaining_verify_v15.json`)
- ADV 런타임 사본 34개 패치·검증, 의도한 스트림 외 바이트 무변경
- `BATTLE BANK IMAGE VERIFY OK` (SLGRES 287 / SLGSTAGE 1,078)
- 폰트는 무변경 — 새 한글이 전부 기존 1,251글리프 안에 있어 ELF SHA-256이 v1.4와
  동일한 `60b38329333579e90f224abedbe18786b4b1d3be365aa7ead63fa74b0f901bb6`

**교훈:** 정지 원인을 레이아웃(컨테이너 위치)으로 지레짐작하지 말 것. 세이브스테이트
EE 메모리를 일본어판 상태와 나란히 놓고 보니 스크립트는 멀쩡했고, 진짜 원인은
번역문이 만든 **빈 줄**이었다.

## 2026-09-03 렌더러 결함 수정과 재출시 (v1.4)

Moonlit Lovers 작업 중 같은 렌더러를 쓰는 이미지들을 대조하다가, **v1.3 출시본에 들어간
렌더 결함 6종**을 발견했다. 원인은 모두 하나로 모인다: 마스크가 라벨이 아니라 **판 전체나
테두리를 먹어서**, 지우기와 글자 크기가 둘 다 어긋난 것이다.

| 대상 | v1.3 증상 | 수정 |
|---|---|---|
| `gowin00` 설정창 | 거대한 「게임 설정」이 창을 덮고 일본어는 그대로 | 탭 캡션 2곳만 실측 사각형으로 교체 |
| `gebtn00~04` 11장 | 버튼 테두리가 지워지고 글자가 판을 넘침 | 라벨 상자(`region`) 지정, 흐린 사본은 `mask_from` |
| `ggmenu_btn*` 34장 | 일본어 유령이 남고 글자가 가운데로 밀림 | `centre_on_band`, 흐린 `b` 사본은 밝은 형제에서 마스크 차용 |
| `ggunit_actset*` 6장 | 배지 테두리 파괴, 글자 뭉개짐 | 안쪽 상자로 한정 + `mask_from` |
| `help_00~05` 6장 | 판독 불가 — 일본어 위에 한글이 겹침 | 글리프 모드 + 줄별 영역, △ 아이콘 보존 |
| `gwp16` 명령 범례 | 9줄이 거대한 가운데 정렬로 뭉개짐 | 줄별 영역 + 영역별 임계(`threshold_per_region`) |
| `grbtn1`/`gseff08`/`gybtn00` | 「닫기る」처럼 꼬리가 남음 | 글리프 모드 + 아이콘 오른쪽 영역 |
| `gibtn0f`/`gubtn0f`/`gxbtn3f`/`gybtn11b·f` | 밝은 사본에서 판 전체가 마스크됨 | 어두운 형제에서 마스크 차용 |

### 렌더러 자체 개선 (`tools/eternal_lovers_render_candidate_images.py`)
- `mask_from`: 같은 그림의 다른 밝기 사본에서 마스크를 빌려온다. 흐린 버튼은 글자와 판이
  한 단계밖에 차이나지 않아 어떤 임계로도 분리되지 않는다.
- `erase_label`: `cv2.inpaint` 대신 **가로·세로 이웃을 거리 가중 평균**해 배경을 복원한다.
  버튼 막대는 옆 픽셀만 쓸 수 있고, 테두리 있는 목록은 위아래 여백이 진짜 배경이다.
- `threshold_per_region`: 캡션마다 밝기가 다를 때(회색 처리된 줄)만 켜는 옵션.
- 글리프 모드도 `region`이 있으면 글자 폭을 그 상자로 제한한다.

### 표기 통일
`戻る` 4곳(`gxbtn3`, `gxbtn3f`, `gybtn00`, `gxwin02`)을 모두 **「뒤로」**로 통일했다.
`gxwin02`의 슬롯은 54px라 「돌아가기」가 아이콘과 창틀을 모두 넘어갔고, Moonlit 쪽 기존
번역도 같은 이유로 「뒤로」를 쓰고 있었다.

### 검증과 릴리스
- 263장 전수 재렌더 → v1.3과 **85장이 달라짐**, 33장 대조 시트로 원본/기존/신규 전수 확인
- `BATTLE BANK IMAGE VERIFY OK`, ISB 134 리소스, 나머지 텍스트 SLG 424 / SLGSTAGE 1,258 검증
- 최종 ISO SHA-256 `817298cf279f064c4ee562dcdd8325cfb1e0649eba30890d3bbeb4a94961042d`
- 릴리스 `release/galaxy_angel_eternal_lovers_ps2_kr_v1.4.xdelta`
  (SHA-256 `e00cb96316a2325f41c4284391dff7199d3eea4b58a30f0fda0087f3f928f4c4`, apply-check 통과)

**교훈:** 렌더 결과를 "원본 옆에 나란히" 두고 보지 않으면 이런 결함은 통과한다. v1.3까지는
번역문만 확인했고, 실제로 게임 화면에서 어떻게 보이는지는 대조하지 않았다.

## 2026-09-03 이미지 전수 감사와 번역 결함 수정

### 고유 이미지 4,976장 시각 감사 (감사 시트 156장)

- `assets/translation/images/image_units.json`의 고유 이미지 전량이 `pending_visual_audit`
  상태로 남아 있었다. 로컬 비전 모델(LM Studio `qwen3.8-27b`)로 156개 감사 시트를 전수
  검사했다. 도구: `tools/eternal_lovers_audit_images_local_ai.py`
  - 결과: `analysis/image_visual_audit.json` (히트 515장)
  - 대조: `tools/eternal_lovers_reconcile_image_audit.py` → `analysis/image_audit_reconcile.json`
- **재현율 측정**: 이미 번역한 301장 중 226장을 다시 검출해 **75.1%**. 즉 이 감사는
  완전 탐지가 아니라 하한선이며, 아직 못 찾은 일본어 이미지가 남아 있을 수 있다.
- 신규 후보 289장을 계열별 시트로 만들어 **육안으로 전수 판정**했다. 판정 결과는
  `assets/translation/images/candidate_translations.json`에 원문/번역/렌더 힌트와 함께
  기록했다(렌더 188 / 글자타일 9 / 제외 21 / 보류 23).

### 삽입 이미지 329장 → 620장

| 컨테이너 | 이전 | 현재 |
|---|---:|---:|
| GADAT030 | 76 | 152 |
| GADAT032 | 253 | 309 |
| SLG | 0 | 159 |

- **`haplc_*` 77장**: `gaplc_*`와 같은 번호·같은 장소명을 검은 외곽선 스타일로 그린
  두 번째 명판 세트인데 통째로 미번역이었다. 기존 장소명 번역을 그대로 재사용해 렌더했다.
  (`tools/eternal_lovers_render_haplc.py`)
- **전투 UI 등 신규 188장**: 유닛·함급·소속·파일럿 이름판, 전투 메뉴 버튼, 승리/패배 조건,
  시스템 대화상자, 설정 화면 등. 원문이 이미 번역된 `.tbl` 문자열과 일치하면 그 번역을
  재사용해 이름 표기가 전투 로그와 어긋나지 않게 했다.
  (`tools/eternal_lovers_render_candidate_images.py`,
  `tools/eternal_lovers_match_image_text.py`)
- **앨범 제목 글자 타일 39장(9개 제목)**: 가나 한 글자당 텍스처 1장인 그룹.
  1편에서 확정한 규칙대로 **장수를 유지하고 남는 타일은 완전 투명**으로 채웠다. 장음 부호
  타일처럼 물리적으로 작은 텍스처에는 음절을 배치하지 않는다.
  (`tools/eternal_lovers_render_album_letter_tiles.py`)
- **PSMCT16(16비트 RGB5A1) 텍스처 코덱 추가**: `haplc_*`가 쓰는 포맷을 공용 코덱이 읽지도
  쓰지도 못했다. `tools/galaxy_angel_gadat032.py`에 디코더/인코더를 넣었고 원본 왕복이
  바이트 단위로 일치한다.

### Moonlit Lovers에서 상속한 이미지의 결함 정리

픽셀 해시로 가져온 171장을 전수 대조한 결과 상당수가 Eternal Lovers에 맞지 않았다.

- **`gmplc_pop*` 25장**: 방 미리보기의 **방 그림 자체가 뭉개져** 있었다(텍스처 전체를 텍스트
  판으로 처리한 결과). 캡션 줄만 교체하는 렌더러로 다시 만들어 그림을 보존했다.
  덤으로 게스트룸이 "오리지널 노아의 방", `???`가 "치토세의 방"으로 표시되던 캡션 오류도
  바로잡았다. (`tools/eternal_lovers_render_room_popups.py`)
- **`gmblk_btn*` 12장 제거**: 원본이 라틴 문자(`A BLOCK`)뿐인데 한글이 덧그려져 있었다.
  일본어만 번역한다는 방침에 따라 삽입 대상에서 뺐고, 재동기화 시 다시 들어오지 않도록
  `tools/eternal_lovers_sync_shared_japanese_images.py`에 제외 목록과 보호 로직을 넣었다.
- **텍스트 판 19장 재렌더**: `ギブアップ`가 "랭킹"으로 오역돼 있었고, `gproute*` 이름판은
  가나가 절반만 지워져 `ミル밀피유`처럼 남아 있었으며, 루트 시작 확인창 6종은 2줄 원문을
  1줄로 줄여 질문 줄이 사라져 있었다. `gdmes47/53`은 다른 메시지의 문구를 달고 있었다.
- **시스템 확인창 2장**: 덮어쓰기 창이 `※이전 데이터는 사라집니다` 경고를 잃고, 삭제 창은
  원문에 없는 그 경고를 달고 있었다. Eternal 원문 기준으로 다시 렌더했다.

### 텍스트 번역 결함 238건 수정

`tools/eternal_lovers_fix_isb_translations.py` (보고서: `analysis/isb_translation_fixes.json`)

- 표기 정정 233건: `브릿지`→`함교` 213, `엘시올`→`에르시올` 9, `크레이타`→`크레타` 6,
  `트랜스바르`→`트랜스벌` 3, `알모`→`아르모` 1, `스카이팔스`→`스카이팔레스` 1
- **`needs_human` 플레이스홀더 3건이 실제로 게임에 삽입되고 있었다.** 원문이 CP932로
  깨진 바이너리 레코드라 번역 대상이 아니며, 원본 바이트 유지로 전환했다.
- 앞 줄 번역이 그대로 복사된 오역 1건과, 표기 교정으로 슬롯을 넘긴 1건을 수동 교정했다.
- 비-ISB 텍스트 3건, 장소명판 이미지 14건(`하얀 달`→`백색 달`, `고래방`→`쿠지라 룸`,
  `타쿠토`→`택트`, `브라망슈`→`브라만슈`, `아드미랄`→`아드미럴`)도 같은 기준으로 고쳤다.
- 검증기(`tools/eternal_lovers_verify_isb.py`)가 의도적 미번역을 결함으로 세지 않도록
  `deliberately_untranslated_units`를 분리했다. 결과: **overflow 0 / 누락 글리프 0 /
  build_ready=True**.

### 렌더 품질 결함 수정

- **챕터 제목 17장**: 배경을 17장 중앙값으로 합성하던 방식 탓에 청록 얼룩이 남고 글자도
  원본보다 작았다. Eternal의 챕터판은 검은 배경에 청록 글로우이므로, 검은 판 위에 글로우를
  재현하는 전용 렌더러로 바꿨다. (`tools/eternal_lovers_render_chapter_titles.py`)
- **`・`(U+30FB) 두부 현상**: Malgun Gothic에 글리프가 없어 `발□파스크 대함대`처럼 네모로
  구워져 있었다. 공용 렌더러가 그릴 때만 `·`(U+00B7)로 대체하도록 했다(번역 데이터는 원문 유지).

### 빌드 재현성

`tools/eternal_lovers_build_images.py`가 이제 원본 ISO만 있으면 **번역 데이터에서 이미지까지
전부 다시 생성**한다: 이미지 렌더러 6종 → 폰트 → ISB 패치 → SCENARIO 재압축 → GADAT030 /
GADAT032 / SLG 이미지 삽입 → ELF 교체 → 비-ISB 텍스트 삽입.

### 이번 패스에서 넣지 않은 이미지

- **라틴 문자 전용 21장** — 정책상 번역 대상 아님(`APPENDIX`, `BGM`, `ON`, `S.P.W. HYPER
  CANNON` 등).
- **튜토리얼 스크린샷 7장**(`gjbk*i`, 640×480) — 일본어가 게임 UI의 사진으로 구워져 있어
  텍스트 레이어 교체가 아니라 아트 리터칭이 필요하다.
- **캐릭터 선택 행·일부 버튼 15장**(`gybtn*`, `gneff08`, `gpretry`) — 밝은 색 막대 위의 흰
  글자라 어떤 임계값으로도 원문이 깨끗이 지워지지 않았다. 일본어를 반쯤 남긴 채 한글을
  덧그리는 것보다 원본 유지가 낫다고 판단해 제외했다.
- **다중 셀 창 8장**(전투 결과·랭킹·유닛 정보·조건 패널·장식 로고) — 셀 단위 작업 필요.
- 위 목록은 `assets/translation/images/candidate_translations.json`에 사유와 함께 남아 있다.

### 남은 품질 과제

- 본편 대사 36,034단위 중 21,412단위가 아직 `draft` 상태다(로컬 모델 초벌). 표기·플레이스홀더
  결함은 이번에 정리했지만 문맥 검수는 하지 않았다.
- 고정 슬롯에 맞추느라 띄어쓰기가 제거된 대사 341줄이 있다(`백색달로돌아가는것뿐이다` 등).
- 이미지 감사 재현율이 75.1%이므로 미검출 일본어 이미지가 남아 있을 수 있다. 더 높은 배율의
  2차 스크리닝이 필요하다.

갱신: 2026-09-01 (ISB 본편 대사 번역·최종 후보 ISO 정적 검증 완료)

## 2026-09-03 전투 리소스 뱅크 사본과 2차 감사

사용자가 `SLG/png/dat/slg/2dparts`, `SLGRES/png`, `SLGSTAGE/png`에 일본어 이미지가 남아
있다고 지적해 확인한 결과, 두 가지가 드러났다.

### 1) 분류 누락은 아니었다

세 폴더의 PNG는 전부 감사 인벤토리에 들어 있다(SLG 1,724 / SLGRES 1,421 / SLGSTAGE 7,063 —
디스크 파일 수와 정확히 일치). **그 컨테이너에만 존재하는 고유 이미지는 0장**이며, 전부 이미
분류·감사한 이미지의 복사본이다.

### 2) 복사본에 한글이 들어가지 않고 있었다 (실제 결함)

번역본을 `SLG`(+`ADV` 런타임 사본)에만 써넣어, **SLGRES 227곳·SLGSTAGE 770곳의 복사본에
일본어가 그대로 남아 있었다.** 전투 중 스테이지별 뱅크에서 로드되면 화면에 보인다.

원인은 두 겹이었다.

- `--runtime-container` 사본 추적은 **압축 바이트가 동일한 복사본만** 찾는다. 같은 원본
  데이터를 다르게 압축해 둔 뱅크 약 140곳은 발견되지 않았다.
- 이 두 컨테이너는 PIDX가 아니라 **FSTS 색인**이라 이미지 패처의 주 컨테이너 경로로는 아예
  주소를 지정할 수 없다(`manifest offset is not a PIDX record`).

그래서 각 뱅크의 FSTS 레코드를 통해 직접 기록하는 전용 패스를 만들었다.

- `tools/eternal_lovers_mirror_battle_images.py` — 원본 raw 해시가 같은 뱅크 자원을 찾아
  번역 이미지 세트를 컨테이너별로 생성 (SLGRES 284 / SLGSTAGE 1,001)
- `tools/eternal_lovers_patch_battle_bank_images.py` — 뱅크 자체의 원본에서 TEX를 재인코딩·
  재압축해 슬롯에 쓰고 FSTS 레코드의 raw/compressed 크기를 갱신한다. 슬롯이 모자라면 잘라
  넣지 않는다.
- **슬롯별 맞춤 렌더**: 뱅크 슬롯은 원래 일본어 크기에 맞춰져 있어, 같은 높이의 한글이
  수십 바이트 더 크게 압축되는 경우가 있다(8곳). 이름판을 전역으로 줄이면 주 텍스처와
  여유 있는 스테이지 뱅크 1,001곳까지 나빠지므로, **좁은 슬롯에 들어가는 사본만** 조금 작게
  다시 그린다. 결과: SLGRES 284/284, SLGSTAGE 1,001/1,001 — 사본 누락 0.
- 부수적으로 `--no-central-idx`를 이미지 패처에 추가했다. IDX.DAT에 색인이 없는 컨테이너에만
  쓰며, 실제로 색인이 있으면 오류를 내 잘못 사용하는 것을 막는다.

### 3) `dat/slg/2dparts` 2차 스크리닝

1차 감사의 재현율이 75.1%였으므로 같은 폴더에서 미검출로 남은 703장을 **시트당 12장으로
크게 확대해** 다시 검사했다(59시트).

- 결과: `analysis/image_visual_audit_2dparts.json` — 47장 추가 검출
- 육안 판정 후 **38장을 추가 번역**했다(택트・마이어스, 란파・프란보와즈, 레스터・쿨다라스
  이름판, 유닛 이름판의 다른 크기 변형, `자동색적`·`반격`·`작전 정보` 등 명령 버튼,
  승리/패배 조건 2종). 7장은 텍스트 없는 아이콘·스프라이트, 2장은 다중 셀 화면으로 보류했다.
- 이로써 SLG 삽입 이미지가 159 → **196장**이 되었다.

**남는 한계**: 2차 스크리닝은 `dat/slg/2dparts`에만 적용했다. `dat/slg/model`(563장)은 3D
모델 텍스처라 제외했고, 다른 컨테이너의 미검출분에는 같은 2차 패스를 아직 돌리지 않았다.

## 2026-09-03 보류 34장 처리 완료

이전까지 "보류"로 남겨 둔 34장을 전부 처리했다. 남은 보류는 **0장**이다.

### 캐릭터 선택 행 17장 — 원인은 색이었다

"밝은 막대 위 흰 글자라 분리 불가"로 판단했던 것이 틀렸다. 확대 격자를 씌워 실측해 보니
글자는 흰색이 아니라 **막대보다 어두운 남색·진홍색**이었고, 그래서 whiteness 계열 임계값이
전부 실패했던 것이다(어두운 변형에서는 흰 픽셀이 0개였다).

- 라벨 사각형을 실측해(504×68 기준 x 126–258, y 17–47) 그 영역만 주변 막대 색으로 다시 칠하고
  한글을 그린다. 아래의 로마자 이름과 오른쪽 영문 기체명은 건드리지 않는다.
- 글자색은 "막대에서 벗어난 픽셀 중 **어두운 쪽 무리**"의 중앙값으로 잡아 원본과 같은
  진한 색으로 그린다.

### 렌더러 결함 3건을 추가로 고쳤다

- **인페인팅이 투명 픽셀의 검은 RGB를 끌어왔다** — 지운 글자 자리에 검은 유령이 남았다.
  투명 영역을 가시 색으로 채운 뒤 인페인트한다.
- **글자에 딸린 어두운 외곽선이 마스크에서 빠졌다** — 본체만 지워 외곽선 모양의 잔상이 남았다.
  마스크를 2픽셀 확장한다.
- **영역을 지정해도 글자 폭 예산이 캔버스 기준이었다** — 지정한 사각형을 넘어 그려졌다.
  영역이 있으면 그 폭 안으로 가둔다(`confine`).
- 아울러 밝은 파랑 글자를 위해 휘도 기반 마스크를, 아이콘 옆 라벨을 위해 글리프 모드의
  보호 영역 복사를 추가했다.

### 다중 셀 화면 — 라벨 자동 검출

전투 결과표·랭킹판·기체 정보 화면 등은 라벨이 최대 21개다. 좌표를 손으로 재는 대신
`tools/eternal_lovers_map_window_labels.py`를 만들어 흰 글자를 검출하고 가로로 병합해
캡션 단위 블록으로 묶은 뒤, 읽는 순서(행 → 열)로 정렬해 번역과 짝지었다. `--check`로 번호를
얹은 미리보기를 만들어 짝이 맞는지 **눈으로 확인한 뒤에만** 저장한다.

| 텍스처 | 라벨 |
|---|---:|
| `gpwin0` / `gpwin1` 전투 결과표 | 각 16 |
| `gubase` 기체 정보 | 21 (◀▶ 화살표는 제외) |
| `gwp15` 행동 조건 패널 | 6 |
| `gzbase` 승패 조건 | 2 |
| `gibase` 작전 정보 | 3 (청록 캡션 1개는 검출이 안 돼 사각형을 실측) |
| `grwin0` 랭킹판 | 3 |

### 나머지

- `gfwin03`(데이터 삭제), `gxwin02`(돌아가기), `gneff08`(닫기), `gpretry`(재시도 사용),
  `gybtn11b`(스코어 표시), `ggunit_actset1f`(반격) — 실측 사각형으로 처리
- `gainfo_win00`·`gainfo_win00b`는 확대해 보니 **라벨이 전부 라틴 문자**였다(감사 모델의 오탐).
  번역 대상 아님으로 정정.
- `grttl2`·`gyttl01`은 가나를 기울어진 금색/녹색 판에 한 글자씩 그린 **장식 로고**다. 같은
  제목이 이미 텍스트로 표시되는 곳(`grttl0`, `gyeff` 글자 타일)에서는 한글이므로, 반쯤 지워진
  로고를 만드느니 원본 아트를 유지한다.

## 2026-09-03 나머지 컨테이너 2차 스크리닝

`dat/slg/2dparts` 외에 아직 재검사하지 않은 미검출 **3,683장** 전부를 같은 방식(시트당 12장·
최대 4배 확대)으로 다시 훑었다. 307시트, 오류 0.

- 결과: `analysis/image_visual_audit_pass2.json` — 60장 검출
- 그중 35장은 이미 번역한 리소스(주로 `haplc_*`)의 감사 단위였다. **진짜 신규는 25장**.
- 육안 판정: **번역 8장 / 보류 8장 / 제외 10장**
  - 번역: `gebtn00b`·`gebtn00f`·`gebtn01f`·`gebtn03f`·`gebtn04f`(세이브·로드 버튼의 상태
    변형), `gswin1`(란파 호감도 창), `gdmes31`·`gdmes54`(메모리 카드 용량 경고 2종)
  - 보류: 캐릭터 선택 행 5장(밝은 막대 위 흰 글자), 아이템/오브젝트 선택 창 2장, 맵 창 1장
  - 제외: 타이틀 로고·앨범 썸네일·얼굴 아이콘·이펙트 스프라이트 등 10장
- 구간별 검출 분포를 보면 `gadat031`(얼굴 1,742장)과 `dat/slg/model`(563장)에서는
  사실상 텍스트가 나오지 않았다(각 1장, 2장이며 모두 오탐). 이전 세션의 "GADAT031에는
  텍스트 없음" 판정이 재확인됐다.

### 빌드 단계 순서 결함

전투 뱅크 이미지 검증기(`tools/eternal_lovers_verify_battle_bank_images.py`)가
`battle-bank protected padding changed: SLGSTAGE:block_00005850.png`으로 빌드를 세웠다.

원인은 단계 순서였다. 뱅크 이미지 삽입이 **잔여 텍스트 패스보다 먼저** 실행되는데, 그 패스가
같은 FSTS 뱅크를 재패킹하면서 자원 18,493개를 재배치한다. 그 결과 이미지가 0으로 비워 둔
슬롯 꼬리에 다른 데이터가 들어와 "슬롯 패딩은 0" 불변식이 깨졌다.

→ 뱅크 이미지 삽입을 **잔여 텍스트 패스 뒤로** 옮겼다. 이제 이미지는 최종 레이아웃에 맞춰
기록되고, 이후에 그 영역을 건드리는 단계가 없다. 재빌드 후 `BATTLE BANK IMAGE VERIFY OK`.

### 렌더러 회귀 하나를 함께 고쳤다

`gswin*`(호감도 창) 6장에서 `好感度` 라벨을 지우면서 **옆의 게이지 눈금까지 함께 지워지고**
한글이 게이지 위에 겹쳐 찍히고 있었다. 줄마다 영역을 지정할 수 있게(`regions`) 렌더러를
고쳐, 라벨은 제자리에 그리고 게이지는 보존한다.

## 2026-09-03 최종 ISO와 검증

- 최종 ISO: `build/Galaxy_Angel_Eternal_Lovers_KO.iso`
  - 크기: 4,722,749,440 bytes
  - SHA-256: `794b759b2174693746194dfe28c7e2bb16c60113e959d3d3f80da44f613dff97`
- 삽입 이미지 **2,060곳** (고유 텍스처 695장 + 전투 뱅크 사본 1,365곳)

| 컨테이너 | 삽입 |
|---|---:|
| GADAT030 | 152 |
| GADAT032 | 343 |
| SLG | 200 |
| SLGRES | 287 (사본) |
| SLGSTAGE | 1,078 (사본) |

- 검증 결과
  - 본편 ISB: 134개 ISO 내부 자원을 다시 해제해 빌드 자원과 **전수 일치**
  - 고정 슬롯 재검증: 단위 35,932 / 번역 35,929 / 원본 유지 3 / **오류 0, 슬롯 초과 0,
    누락 글리프 0, build_ready=True**
  - 비-ISB 텍스트: 1,706개 리소스 / 590,058개 문자열 기대 바이트와 전수 일치
  - PIDX 컨테이너 이미지 695장: ISO에서 다시 읽어 픽셀 검증 통과
  - 전투 뱅크 사본: **BATTLE BANK IMAGE VERIFY OK** — SLGRES 287/287, SLGSTAGE 1,078/1,078
- 이미지 판정 최종 집계: **렌더 263 / 글자 타일 9 / 제외 42 / 보류 0**
- 대조 시트: `build/image_compare/`

## 2026-09-03 릴리스 패키지 (v1.3)

`tools/eternal_lovers_make_release.py` → `release/`

- `galaxy_angel_eternal_lovers_ps2_kr_v1.3.xdelta` (56,472,522 bytes)
  - SHA-256: `aec304f7725ceb5d06623d436a175bbf6cece57f2dceb05d34a5e0f83994d745`
- `README.txt`, `SHA256SUMS.txt`, `release.json`
- **적용 검증 완료**: 패치를 원본 ISO에 다시 적용해 나온 ISO가 검증한 최종 ISO와 SHA-256까지
  일치함(`apply_check.matches: true`).
- 지원 원본 ISO
  - 크기 4,662,525,952 bytes / SHA-256
    `31cb2a0b6a219323ea8fc451050a75f06fc0947fb0ff33b182835adf7b6da25d`
- 버전 이력: v1.0 최초 릴리스 → v1.1 전투 뱅크 사본 삽입·`2dparts` 2차 감사 →
  v1.2 나머지 컨테이너 2차 감사·`gswin` 회귀 수정·빌드 순서 수정 →
  v1.3 보류 34장 전부 처리(캐릭터 선택 행 17장, 다중 셀 화면 7장, 소형 칩·창 8장)

### 남은 미번역 (제외 42장, 사유별)

| 사유 | 장수 |
|---|---:|
| 얼굴 아이콘·포트레이트·이펙트 스프라이트 등 텍스트 없음 | 23 |
| 라틴 문자 전용(APPENDIX, BGM, ON, INFORMATION SCREEN, 타이틀 로고 등) | 10 |
| 튜토리얼 스크린샷(일본어가 UI 사진으로 구워짐) | 7 |
| 장식 로고(기울어진 판에 한 글자씩; 같은 제목이 다른 곳에서는 한글) | 2 |

`assets/translation/images/candidate_translations.json`에 항목별 사유가 남아 있다.

## 2026-09-01 최종 후보 ISO 완료

- 본편 ISB 번역 단위 35,932개를 모두 번역하고 58,218개 원본 저장 레코드에
  분배했다. 미번역 0개, 누락 글리프 0개, 고정 슬롯 초과 0개다.
- `tools/eternal_lovers_verify_isb.py`를 실제 패처와 같은 개별 슬롯 정렬 판정으로
  통일했다. 총 바이트 수뿐 아니라 2바이트 글리프가 레코드 경계에서 만드는
  1바이트 정렬 손실도 검증한다.
- `tools/eternal_lovers_patch_isb.py`로 134개 SCENARIO 리소스의 35,932개 단위를
  원래 블록 키로 재암호화하고 즉시 정적 복호화해 왕복 일치를 확인했다.
- 최종 글꼴은 사용자 글리프 1,251개를 사용했다(가용 1,352개).
  - ISO 내부 ELF SHA-256:
    `60b38329333579e90f224abedbe18786b4b1d3be365aa7ead63fa74b0f901bb6`
- 최종 후보 ISO:
  - `build/Galaxy_Angel_Eternal_Lovers_KO.iso`
  - 크기: 4,722,841,600 bytes
  - PVD 논리 크기: 4,722,841,600 bytes (파일 크기와 일치)
  - SHA-256:
    `e591ff53bb79bfdf6ff2a6b272e893ddc2bdcb9eabc141f3008c67d96133c540`
- 정적 검증:
  - ISB 134개 ISO 내부 자원을 재압축 해제해 빌드 원시 자원과 전수 일치
  - 비-ISB 1,706개 자원 / 590,058개 문자열 기대 바이트와 전수 일치
  - 이미지 329개 쓰기 후 검증 통과
  - ISB 구조 오류 0, 슬롯 초과 0, 누락 글리프 0
- 사용자 요청에 따라 이번 완료 과정에서는 에뮬레이터 실행·메모리 추적 등
  동적 분석을 수행하지 않았다.

## 2026-09-01 ISB 정적 분석 전환 후 진행

- 사용자 요청에 따라 이후 작업은 추출 파일과 `SLPM_658.78`만 사용하는
  **정적 분석**으로 한정했다. 에뮬레이터 실행·메모리 추적은 더 하지 않는다.
- `SLPM_658.78`의 MIPS64 코드를 정적으로 디스어셈블해 `0x00080002` 문자열
  상수 처리 분기와 복호 루프를 찾았다.
  - 핵심 루프: `0x0025C158`~`0x0025C178`
  - 32비트 암호문 워드마다 `ROR32(word, 3) XOR block_key`
  - 같은 루프의 두 문자열 객체 경로: `0x0025BFE8`, `0x0025C220`
- ISB 하나에는 키가 하나만 있는 것이 아니라 여러 시나리오 블록이 들어 있고,
  블록 키는 파일 첫 워드 또는 블록 앞의 직렬화된 `0x00000401` 정수 리터럴에
  저장된다. 인접 문자열 군을 CP932로 복호화했을 때의 품질을 비교해 실제 키
  전환만 선택하는 정적 키 경계 복구기를 구현했다.
- `tools/eternal_lovers_isb_static.py`:
  - `02 00 08 00 + length + payload` 문자열 레코드 파싱
  - MIPS 루프와 동일한 복호·재암호
  - 블록 키 경계 자동 복구
- `tools/eternal_lovers_extract_isb.py`로 224개 SCENARIO 리소스를 검사했다.
  - 태그 문자열 레코드: 82,910개
  - 안전하게 CP932 복호·분류한 레코드: 82,352개
  - 번역 대상 일본어 단위: 35,932개 / 134개 리소스
  - 연속된 4바이트 정렬 문자열 조각은 하나의 문장으로 다시 결합했다.
  - 결과: `assets/translation/isb/index.json`, `assets/translation/isb/segments/*.json`
- 첫 리소스의 정적 복호 검증 예:
  - 파일 선두 키 `0xB7F1C996`: 프롤로그 내레이션과 `GC000000.ogg` 계열
  - `0x00000B04` 키 `0x424D3772`: `GC000100.ogg`, `タクトさ～ん。` 이후
  - 이어지는 미르피유/란파/민트/포르테/바닐라/치토세 분기 키도 전부 자동 복구
- 앞선 두 작품과 평탄화한 일본어 원문 및 번역이 모두 유일하게 일치하는
  1,288개 단위(고유 원문 410개)는 기존 번역을 안전하게 재사용했다.
- 로컬 번역 모델을 쓰는 기존 번역기에 ISB 고정 슬롯의 `max_bytes` 전달과 동일
  원문 번역 재사용을 추가했고, 전체 35,932개 단위 번역을 완료했다.
- `tools/eternal_lovers_patch_isb.py`를 추가했다. 승인된 번역을 고정 슬롯에 맞춰
  사용자 정의 글꼴 코드로 인코딩하고 원래 블록 키로 재암호하며, 삽입 직후 다시
  정적 복호화해 바이트 일치를 검사한다.
- `tools/eternal_lovers_build_images.py`의 재현 빌드 순서에 ISB 원시 리소스 생성과
  `SCENARIO.DAT` 재압축·재삽입 단계를 연결하고 최종 후보 ISO를 생성했다.

## 2026-09-01 이어서 진행한 작업

### Eternal Lovers 전용 본문 폰트 경로 확정 및 한글 글리프 빌드

- 첫 대사 세이브 상태에서 평문 복사본 3곳을 동시에 감시해 실제 소비 버퍼가
  `0x00F30E22`임을 확인했다.
- 런타임 호출 경로:
  - `0x001674C8`: 대사 표시 상태 갱신(문자열 길이 확인 포함)
  - `0x00168490`: 실제 문자 순회/렌더 함수
  - `0x001685EC`: 현재 문자 바이트 `lbu`
  - `0x001985C8`: Shift-JIS 선행 바이트 판정
  - `0x0018C4F8`: Shift-JIS 코드 → 압축 글리프 인덱스/비트맵 주소 변환
  - `0x0018D508`: 글리프 텍스처 생성 및 표시
- 폰트 테이블은 `SLPM_658.78`에 내장되어 있다.
  - ELF VA: `0x00320CC0`
  - 파일 오프셋: `0x00221CC0`
  - 논리 글리프: 24×24, 2bpp, 144바이트
  - 저장 형식: 두 글리프의 2bpp 평면을 하나의 24×24 PSMT4 셀
    (288바이트)에 합친 paired 형식
  - 글리프 수/Shift-JIS 압축 인덱스: 기존 1편과 동일한 7,045개
- `tools/eternal_lovers_font.py`를 추가했다. 초벌 번역에서 CP932로 표현할 수 없는
  한글 837자를 수집해 안전한 후반부 글리프 슬롯 1,352개 중 837개를 교체한다.
  같은 paired 셀의 반대쪽 기존 글리프 평면은 보존한다.
- 산출물:
  - `build/SLPM_658.78.font_wip`
  - `build/font_map.json`
  - 패치 ELF SHA-256:
    `1192d524745d64dc1ca8f7a7b9135e87f6e3b8aec211c46cb3033be1826ba210`
  - 포맷 증거 이미지: `analysis/font_glyph_0035_variants.png`
    (`0x8163`, 말줄임표가 paired normal 해석에서 정확히 복원됨)

### WIP ISO에 한글 폰트 ELF 통합

- `tools/eternal_lovers_build_images.py`가 이미지뿐 아니라 한글 폰트 ELF도 매번
  재생성하고 ISO의 `SLPM_658.78`에 같은 크기로 삽입하도록 확장했다.
- ISO 내부 ELF를 다시 읽어 패치 ELF와 바이트 단위로 동일함을 확인했다.
- 저장된 첫 대사 화면에서 패치 글리프 셀과 `EA A3 EA A2`를 RAM에 주입해
  실제 대사창에 `가각`이 정상 표시되는 것을 PCSX2에서 확인했다.
  - 런타임 증거: `analysis/runtime_font_ga_gak.png`
- 아래 비-ISB 텍스트 삽입까지 포함한 최종 WIP ISO에서도 내부 ELF SHA-256이
  위 패치 ELF와 정확히 일치한다.

### 잔여 비-ISB 텍스트 재삽입 완료

- `tools/eternal_lovers_patch_remaining.py`를 추가해 `remaining_candidates.json`의
  번역을 사용자 정의 Shift-JIS 매핑으로 인코딩하고 PIDX/FSTS 리소스에
  재압축·재삽입한다.
- 적용 결과: 6개 컨테이너, 1,706개 리소스, 590,058개 실제 문자열 위치.
  - `ADV`: 1개 리소스 / 11개 문자열
  - `GADAT000`: 2개 / 30개
  - `SCENARIO`: 18개 / 493개
  - `SLG`: 424개 / 213,189개
  - `SLGRES`: 3개 / 171개
  - `SLGSTAGE`: 1,258개 / 376,164개
- 기존 슬롯을 넘는 Ikusa LZ 리소스에는 최적 경로 압축을 적용했다. `SLG`는
  PIDX 데이터 영역을 섹터 정렬 상태로 재배치하고 컨테이너를 110,592바이트
  확장했으며, `SLGSTAGE`는 72개 FSTS 뱅크 내부의 16바이트 정렬 여유 공간으로
  해결했다. 번역 문장을 자르거나 생략한 리소스는 없다.
- `SLG`의 5,296개 전체 PIDX 레코드를 처리하도록 중앙 `IDX.DAT` 미러 도구의
  기존 64KB 스캔 제한을 제거했다. 변경 레코드 519개를 포함해 전부 검증했다.
- `tools/eternal_lovers_verify_remaining.py`로 원본에 번역을 다시 적용한 기대
  바이트와 ISO 내부 1,706개 리소스를 전수 대조했다.
  - 삽입 보고서: `build/remaining_patch_report.json`
  - 검증 보고서: `build/remaining_verification_report.json`
- `tools/eternal_lovers_build_images.py`는 이제 원본 ISO에서 이미지 329장,
  한글 글리프 837자, 비-ISB 텍스트를 한 번에 재현한다.
- 최종 WIP ISO:
  - 크기: 4,717,391,872 bytes
  - SHA-256: `0e2fbf61bd6a6fb832e1b6b074559d09e4967f52612ad941a0a44d018efdadb9`

## 2026-08-31 이어서 진행한 작업

### 잔여 텍스트 초벌 번역 완료

- `assets/translation/remaining/remaining_candidates.json`의 현재 후보 6,127개를
  다시 추출한 뒤 번역했다.
  - 번역 사용: 6,092개
  - 기존 1편/Moonlit Lovers 번역 메모리 재사용: 243개
  - 신규 초벌 번역: 5,843개
  - 수동 검토 확정: 6개
  - 내부 리소스 경로·폰트명·더미 등 원문 유지: 35개
- 재실행 가능한 도구:
  - `tools/eternal_lovers_build_glossary.py`
  - `tools/eternal_lovers_translate_remaining.py`
  - `tools/eternal_lovers_finalize_remaining.py`
- 용어집은 Moonlit Lovers 승인 용어를 기반으로 Eternal Lovers 신규 용어
  (루샤티, 바인, 게른, 스카이팔레스, 기어・게른 등)를 더해 71개 항목으로 만들었다.
- 이 번역은 아직 **초벌(draft)** 이다. 특히 전투 문장 조각의 앞뒤 연결과 글상자
  길이는 실제 삽입 후 전수 검수가 필요하다.

### 이미지 329장 ISO 삽입 및 실기 검증

- GADAT030 지명판 76장과 GADAT032 시스템/UI·BGM·챕터·스테이지 이미지
  253장을 TEX/RAT로 재인코딩해 원본 컨테이너와 `ADV.DAT` 런타임 사본에 삽입했다.
- `tools/galaxy_angel_patch_gadat032_images.py`를 다음과 같이 일반화했다.
  - `--primary-container`로 GADAT030/GADAT032 선택
  - Eternal Lovers의 offset 기반 PNG 이름을 `--image-manifest`와
    `--resource-manifest`로 실제 리소스 경로에 연결
- 모든 329개 주 컨테이너 블록과 발견된 런타임 사본을 삽입 직후 다시 해제해
  바이트/픽셀 검증을 통과했다.
- WIP ISO:
  - `build/Galaxy_Angel_Eternal_Lovers_KO_wip.iso`
  - 현재 크기/해시는 위 "잔여 비-ISB 텍스트 재삽입 완료" 절 참고
- 전체 이미지 ISO는 `tools/eternal_lovers_build_images.py --original-iso <원본 ISO>`로
  동일하게 다시 만들 수 있다(원본 SHA-256 검증 포함).
- PCSX2/emucap에서 WIP ISO를 직접 부팅했고 타이틀 메뉴가
  `처음부터 / 이어하기 / 설정`으로 표시되는 것을 확인했다.
  - 증거 캡처: `build/runtime_ko_title.png`
  - 삽입 보고서: `build/gadat030_image_patch_report.json`,
    `build/gadat032_image_patch_report.json`

### ISB 런타임 추적에서 확보한 새 단서

- 첫 대사 원문
  `かつて、この銀河に栄えた文明ＥＤＥＮ（エデン）が、時空震（クロノ・クェイク）によって崩壊してから、数百年……。`
  이 실제 대사창에 표시되는 상태를 확보했다.
- 첫 줄 CP932 평문은 EE RAM에서 3곳에 존재했다.
  - `0x00F30B74`
  - `0x00F30E22`
  - `0x01F6B2C4`
- 파서가 만든 객체에는 `gaf001_01_01` 식별자와 대사 조각, 표시 제어 문자열이
  함께 들어 있었다. 객체 복사에 사용되는 공용 메모리 복사 루틴은
  `0x002A5994` 부근이다.
- 더 이른 생성 단계의 임시 객체는 `0x00F2CD04` 부근에서 관찰됐고,
  실제 첫 대사 화면 세이브 상태를
  `analysis/first_dialogue_trace.p2s`에 저장했다.
- 결론: `.isb` 안의 문장이 단순 평문으로 저장된 것은 아니지만, 실행 중에는
  엔진이 완전한 CP932 평문과 제어 문자열을 객체로 복원한다. 다음 추적은
  `0x00F2CD04` 객체에 일본어 바이트가 채워지는 순간의 **복사 원본 버퍼와 호출자**를
  잡아, ISB 문자열 디코더/문자열 테이블 경계를 특정하는 것이다.

### 현재 WIP ISO에 아직 들어가지 않은 것

- 본편 대사·선택지: ISB 2.0 바이트코드 문자열 형식을 해독해야 한다.
- 따라서 현재 WIP ISO에는 이미지/UI와 비-ISB 전투·설정·함내 설명 텍스트가
  들어갔지만, 본편 대사·선택지가 빠져 있어 전체 한글 패치 완성본은 아니다.

## 원본 검증

- ISO: `D:\game\pcsx2-v2.6.3-windows-x64-Qt\roms\Galaxy Angel - Eternal Lovers (Japan).iso`
- SHA-256: `31cb2a0b6a219323ea8fc451050a75f06fc0947fb0ff33b182835adf7b6da25d`
- `SLPM_658.78` (실행 파일), 총 146개 ISO9660 파일.

## 결론 요약

- **컨테이너 포맷(PIDX0/Ikusa LZSS), IDX.DAT 중앙 인덱스, 텍스처 포맷(TEX/RAT)은
  1편·Moonlit Lovers와 완전히 동일** — 이미지·UI 텍스트 추출은 기존 도구를 그대로
  재사용해 끝냈다.
- **⚠ 대사 스크립트만은 기존 방식으로 추출할 수 없다.** 이 게임은 사람이 읽을 수
  있는 `IDSxxxx:{...}` 텍스트 스크립트 대신, **`ISL 2.0`이라는 컴파일된 바이너리
  포맷(`.isb`, STX 컴파일러 산출물)**을 쓴다. 1편·Moonlit Lovers 파서가 인식하는
  `IDS\d+:\{`, `\Speaker(`, `@N(...@@)` 패턴이 이 게임의 압축 블록 어디에도 없다
  (미디어를 제외한 전 컨테이너 전수 스캔, 0건). 이건 새로운 리버스 엔지니어링
  대상이며, 이번 세션에서는 시도하지 않았다 — 아래 "핵심 발견: 대사 스크립트가
  컴파일된 바이너리" 참고.

## 오늘 완료한 작업

### 1. 시나리오 컨테이너 조사 — SCENARIO.DAT는 스크립트가 아니다

- `SCENARIO.DAT`(3,145,728B, PIDX0, 224블록)를 1편 방식대로 전부 해제해봤지만
  `IDS\d+:\{` 패턴이 0건이었다. `ADV.DAT`(2,029블록), `GAEL.DAT`, `GADAT000.DAT`도
  0건. **미디어(BGM/MOVIE/VOICE)를 제외한 전 컨테이너를 스캔했지만 어디에도
  `IDS` 라벨 스크립트가 없다.**
- `SCENARIO.DAT` 블록 224개 중 19개는 `//` 로 시작하는 ini류 설정 파일이었다.
  그중 하나(`SCENARIO_DAT_002ed000.txt`, 56KB)가 결정적 단서였다:

  ```
  //	ISLシナリオブロックIDテーブル
  //		このファイルはSTXコンパイラによって生成されました。
  //		2004 12/20 20:47
  [FILE]
  ;(block_id):(file_name)
  #0x0842675f=aaaa.isb
  ...
  #0xb78cab9c=6100.isb
  #0x461ba2ad=6101.isb
  ```

  이건 "블록 ID → `.isb` 파일명" 색인 테이블이다. `GAEL.DAT`의 전역 설정도
  `SCENARIO=dat\gadat001`, `TBI=dat\gadat001\` 로 시나리오 폴더를 가리키며,
  `dialog.tbl`/`title.tbl`/`Font.tbl` 등 1편 계열과 같은 이름의 설정 파일들을
  나열한다.
- 즉 **원본 개발사가 이 작품부터 스크립트 엔진을 텍스트 인터프리터에서 컴파일형
  바이트코드(ISL 2.0)로 교체했다.** `SCENARIO.DAT`의 나머지 ~200개 이진 블록이
  바로 이 컴파일된 `.isb` 시나리오들로 추정되지만, 옵코드 포맷을 아직 전혀 모른다.
- `SCENARIO.DAT`의 ini류 블록 중 나머지는 함내 이동 시간표(`TBI_1_02.TBL` 등,
  1편에도 있던 형식)와 함내 장소 설명 39개(`ここはブリッジだ。オレが不在の間は、
  レスターが代わりに指揮をとってくれている。` 등, 1편의 MISC 설명문과 같은 성격)
  였다. 이건 정상적으로 추출했다 — 아래 "2. 잔여 텍스트" 참고.

### 2. 잔여 텍스트(전투/UI/함내 설명) 추출 — 정상 작동

`galaxy_angel_audit_text_coverage.py`의 ini 파서(`#NNN=값` 필드, `[SECTION]`,
`//`/`;` 주석 제거, 10의 배수 필드 제외)는 이 게임에도 그대로 통했다.

- 컨테이너 전수 스캔(미디어 제외) 결과:
  - `SLGSTAGE.DAT`: 376,318회 (전투 메시지, 스테이지마다 반복 정의)
  - `SLG.DAT`: 213,350회
  - `SLGRES.DAT`: 181회
  - `GADAT000.DAT`: 69회
  - `ADV.DAT`: 63회
  - `GAEL.DAT`: 8회
  - **고유 문자열 6,086개**
  - 결과: `assets/translation/remaining/remaining_candidates.json`
- `SCENARIO.DAT`는 스크립트가 아니므로 위 스캔에서 제외했었는데, 별도로 ini
  블록만 훑어 함내 장소 설명 **39개**를 추가로 뽑았다:
  `assets/translation/remaining/scenario_ini_candidates.json`
  (`ここはブリッジだ。...`, `司令官室だ。オレの私室も兼ねている。` 등)

### 3. 이미지(텍스처) 추출 — 1,948개 → 2,809개, 26장 컨택트시트 전수 검수 완료

- `GADAT030.DAT`(581블록/339 PNG), `GADAT031.DAT`(668블록/306 PNG),
  `GADAT032.DAT`(1,802블록/1,332 PNG) 전부 추출. 1편 텍스처 코덱
  (`galaxy_angel_gadat032.py`/추출기)이 수정 없이 통했다.
- `ADV.DAT`의 텍스처류 블록 1,927개는 전량 위 세 컨테이너와 SHA-256 동일한
  런타임 사본 — 중복 검수 불필요.
- **컨택트시트 26장(GADAT030 5장 + GADAT031 4장 + GADAT032 17장) 전부를
  직접 육안으로 확인했다** (환경에 OCR 엔진 없음, 1편·Moonlit Lovers와 동일한
  방식).

카테고리별 발견 (Moonlit Lovers와 거의 동일한 구조이나, 이 게임 전용 콘텐츠 추가):

- **GADAT030 (339개, 지명판)**: 거의 전부 번역 대상. 기존 위치명에 더해
  `スカイパレス・祝賀会場`, `ライブラリ`, `繁華街`, `ギア・ゲルン艦内`,
  `白き月・ドック` 등 이 작품 전용 새 장소가 다수 추가됐다.
- **GADAT031 (306개, 캐릭터 얼굴 아이콘)**: 전 시트 확인 결과 텍스트 없음
  (Moonlit Lovers에 있던 소수의 캡션 카드도 이 게임에는 없었다).
- **GADAT032 (1,332개, UI/시스템, 17장 전수 확인)**:
  - 타이틀/설정 메뉴, 시스템 확인 대화상자(세이브/로드/메모리카드 에러 등)는
    Moonlit Lovers와 문구가 거의 동일
  - **새 기능**: `「一つ前の選択肢まで戻りますか？」`(직전 선택지로 되돌아가기),
    `データをクイックロードします。`/`クイックセーブします。`(퀵세이브/로드)
  - **BGM 목록**에 이 작품 신규 캐릭터 테마 다수: `ルシャーティとヴァイン`,
    `ヴァインその正体`, `支配者ゲルン`, `ヴァル・ファスク大艦隊`,
    `ルシャーティの悲しみ` 등 — **ルシャーティ, ヴァイン, ゲルン**은 이 작품
    전용 신규 캐릭터/세력명으로 보인다 (용어집에 반영 필요)
  - **CG 갤러리 에피소드 제목 17개**: `よみがえる神話`, `破壊者来たりて`,
    `うしなわれるもの`, `エンジェル隊、絶体絶命`, `ギャラクシーエンジェル`,
    `ロストメモリーズ`, `2度目のファーストキス`, `タクト改造計画`,
    `愛のスーパーダイブ`, `こちらミント放送局`, `声が聞こえる`, `存在の役割`,
    `愛する人`, `半分ずつの願い`, `やきもちヒーリング`, `おかしな三角関係`,
    `トラブル・ラブレター`
  - **전투 스테이지/챕터 이름 13개**: `未知の艦隊`, `小型船救出`,
    `ヴァル・ファスク侵攻艦隊`, `侵攻艦隊第2陣`, `ヴァル・ファスク拠点攻略`,
    `紋章機奪還`, `7番機との戦い`, `ヴァインの罠`, `EDEN本星解放作戦`,
    `スカイパレス防衛戦`, `ヴァル・ファスク本星決戦`, `ゲルン艦破壊`
  - 방 아이콘 라벨, 캐릭터 이름표(ミルフィーユ/ランファ/ミント/フォルテ/
    ヴァニラ/ちとせ), 스코어 표시/랭킹 버튼은 Moonlit Lovers와 동일
  - 이미 영문이라 번역 불필요: `AUTO`/`Back`/`Sys`, `GAME OVER`, `Movie`,
    `Other Scenes`, `Score Attack`, `APPENDIX`, `BGM`, `KUJIRA-REPORT`,
    3D 미니게임 방 라벨 대부분(`Vanilla's room`, `Shooting range` 등),
    `ROUND 1/2/3/4/5/9`

### 4. IDX.DAT 중앙 인덱스 / ADV.DAT 사본 관계 확인

- `GADAT030.DAT`의 PIDX 리프 튜플 581개 전부가 `IDX.DAT` 평면 테이블에서
  단일 file_id(22)로 **581/581 완전 일치**. 1편·Moonlit Lovers와 동일한
  `(file_id, offset, raw_size, compressed_size)` 구조 확인.
- `ADV.DAT` 텍스처 블록 1,927개 전량이 GADAT030/031/032와 SHA-256 동일한
  런타임 사본.

### 5. 실기 검증 (emucap + PCSX2)

- 원본 미번역 ISO를 실제로 부팅: 타이틀 화면(`GALAXY ANGEL Eternal Lovers`,
  はじめから/つづきから/設定) → 오프닝 → **タクト의 오프닝 내레이션 대사
  화면**까지 정상 진행 확인.
- 이 결과는 게임 자체는 `.isb` 컴파일 스크립트를 정상적으로 해석해 대사를
  렌더링하고 있음을 보여준다 — 문제는 오직 우리 쪽 정적 추출 도구가 그 바이너리
  포맷을 아직 못 읽는다는 것.
- 세션은 검증 후 정상 종료했다.

## 핵심 발견: 대사 스크립트가 컴파일된 바이너리 (ISL 2.0 / STX)

**이건 1편·Moonlit Lovers 때 쓰던 방식으로는 풀리지 않는, 별도의 리버스
엔지니어링 과제다.** 지금까지 확보한 단서:

- 포맷 이름: `ISL 2.0` (호환성 주석 `ISL2.0 compatible`이 여러 ini 파일에 남아있음)
- 컴파일러: `STX 컴파일러` (`.isb` 확장자 산출)
- 블록 ID(`0x0842675f` 같은 32비트 해시) → 파일명(`6100.isb` 등) 매핑 테이블은
  평문 ini로 존재 — **어떤 시나리오가 어떤 이름인지는 알 수 있지만, `.isb` 자체의
  내부 구조(옵코드, 문자열 테이블, 오프셋 인코딩)는 전혀 모른다.**
- `.isb` 바이너리 자체는 `SCENARIO.DAT`의 나머지 ~200개 압축 블록 어딘가에
  있을 것으로 추정되나, 어느 블록이 어느 `.isb`에 대응하는지는 아직 특정하지
  않았다.
- 이 문제를 풀려면 1편의 "폰트 실체 추적"처럼 emucap으로 실행 중 메모리를
  추적해 스크립트 인터프리터가 `.isb`를 읽는 지점을 찾고, 옵코드를 하나씩
  디스어셈블해야 한다. Ar nosurge 패치에서 "미구현이던 Gust 스크램블 v1을
  디스어셈블로 해독"했던 것과 성격이 같은 작업이며, 텍스트 치환만으로 끝나는
  작업이 아니다.

## 오늘(추가 세션) 완료: 잔여 텍스트 6,086개 검토 — 번역 대상/개발용 분리

`assets/translation/remaining/remaining_candidates.json`의 6,086개 고유 문자열을
전수 검토해, 화면에 나오지 않는 개발용 값(폰트 지정, 리소스 경로, 더미 placeholder)
과 실제 플레이어 노출 텍스트를 분리했다. 판별 기준은 추측이 아니라 기계적 신호다:

- **경로(31개)** — 원문에 `\`(백슬래시)가 있으면 리소스 경로다. 일본어 문장에는
  백슬래시가 나올 수 없으므로 오탐 없음 (`dat\gadat032\章タイトル\gkscr%03d.rat` 등).
- **폰트 패밀리명(1개)** — `"ＭＳ ゴシック"` 하나. 따옴표로만 감싼 값 패턴으로 검출.
- **더미 placeholder(1개)** — `★ダミー★`(그대로 "더미"라는 뜻의 개발용 표시값).

나머지 **6,053개는 전부 실제 플레이어 노출 텍스트**로 확인했다 — 전투 메시지
(`MESSAGE*` 섹션, 압도적 다수), 무기명(`WE*`, 예: `ビーム砲`→빔포), 유닛/함선명
(`UN*`, 예: `戦艦`→전함), 파일럿명(`PI*`), 함내 구역 안내(`BLOCK_*`) 등. 섹션별
빈도 분포와 2글자 이하 극단값(전투 메시지 이어붙는 조각, "攻撃"/"移動"/"待機" 같은
행동 라벨)까지 표본 검토해 확인했다 — 오분류 없음.

- 처리 스크립트: `analysis/classify_remaining.py` (재실행 가능, 원본 파일 갱신)
- 결과: `remaining_candidates.json`에 `use_translation: false` + `exclude_reason`
  필드 추가(경로/폰트/더미 33개), `translatable_strings: 6053` /
  `excluded_dev_strings: 33` 통계 추가.
- `scenario_ini_candidates.json`(함내 장소 설명 39개)도 `remaining_candidates.json`과
  같은 스키마(`id`/`translation`/`state`/`use_translation`)로 맞췄다
  (`analysis/normalize_scenario_ini.py`). 39개 전부 번역 대상 확인됨(개발용 값 없음).

**→ 이 두 파일은 이제 번역해서 `translation` 필드만 채우면 되는 상태다.**
`use_translation: false`인 33개는 그대로 두면 된다(원문 유지).

## 아직 확인하지 못한 것

1. **`.isb` 바이트코드 포맷 자체** — 이게 풀리기 전까지는 이 게임의 본편 대사
   (가장 큰 텍스트 볼륨)와 **대화 선택지**를 추출·번역·재삽입할 방법이 없다.
   1편에서 선택지는 대사 스크립트에 인라인(`@N(...@@)` + `<N->IDS...>` 앵커)으로
   들어 있었으므로, 이 게임도 선택지가 `.isb` 안에 있을 가능성이 높다 — 즉
   선택지 추출은 본편 대사 해독과 **같은 작업**이지 별도 작업이 아니다.
2. ~~**폰트 실체**~~ → 완료. `SLPM_658.78` 파일 오프셋 `0x00221CC0`의 paired
   24×24 2bpp 테이블이며, 한글 837자 패치 ELF까지 생성·ISO 통합했다.
3. ~~`remaining_candidates.json`의 6,086개 분리~~ → 완료 (위 참고).
4. **본편 ISB 재삽입 파이프라인·글상자 실측** — 비-ISB 잔여 텍스트 빌더와
   전수 검증은 완료했다. 본편 `.isb` 해독/빌더와 실제 화면의 문맥·줄 길이
   검수가 남았다.
5. ~~**이미지(텍스처) 번역**~~ → GADAT030 76장과 GADAT032 253장, 총 329장
   삽입 및 런타임 검증 완료.

## 이미지 번역 — GADAT030 함내 장소명판 76개 완료

`assets/image_extraction/japanese_images/GADAT030/`에 1편(`galaxy_angel`)과
같은 방식(`raw`/`png`/`translated_png` + `manifest.json`)으로 작업했다.

- **텍스처 구조 확인**: `gaplc_NNNi.tex`(208×28, 76개)는 배경 없이 텍스트
  글리프만 들어있는 완전 투명 PNG였다(육안으로 "회색 버튼"처럼 보인 건 뷰어가
  투명 영역을 회색으로 채워 보여준 것뿐). 버튼/테두리 프레임은 별도 공유
  텍스처(`gaplc_win.tex`, 248×40)로, 텍스트 없음 — 건드릴 필요 없다.
  같은 컨테이너의 640×480 배경 이미지(`gjbk0NNi.tex`, 262개)는 텍스트가
  전혀 없는 함내 배경 아트라 번역 대상이 아니다.
- **필명→내용 대조 함정**: 자동 생성 컨택트시트(`contact_sheets/`)는 캡션과
  썸네일의 정렬이 어긋나 있어(예: 캡션 열과 이미지 열이 한 칸씩 밀림) 육안으로
  그대로 읽으면 파일명을 잘못 짝짓는다. 실제로 `gaplc_412i`를 컨택트시트로는
  "繁華街"로 잘못 읽었는데, 개별 PNG 직접 열람으로는 "白き月・通路"였다
  (정답은 `gaplc_421i`가 "繁華街"). **그래서 직접 만든 1열 목록(파일명 바로 옆에
  이미지)으로 76개 전부를 다시 확인해 확정했다.** 컨택트시트는 "이 시트에 텍스트가
  있다/없다" 스크리닝용으로만 쓰고, 파일명 대조에는 쓰지 말 것.
- **렌더링**: Malgun Gothic Bold(`malgunbd.ttf`), 원본과 동일한 세로 텍스트
  밴드(y 4~21, 캔버스 28px 중)에 가로 중앙정렬로 맞춰 렌더링. 원본 글자 색상
  `(240,240,240,242)` 유지. 폭이 넘치면 자동으로 폰트 크기를 줄인다(최소
  18pt까지 확인, 가독성 문제 없음).
  - **함정**: Malgun Gothic Bold에 U+30FB(일본어 가운뎃점 `・`)글리프가 없어
    깨진 네모(tofu)로 나온다. 렌더링 시점에만 시각적으로 동일한 U+00B7(`·`)로
    바꿔 그렸다 — **번역 데이터(`gaplc_translations.json`)에는 원문 그대로
    `・`를 유지**했으니 나중에 다른 폰트로 다시 그릴 때 이 문제를 잊지 말 것.
- **결과물**:
  - 번역 데이터: `assets/analysis/gaplc_translations.json` (원문/번역 76쌍)
  - 렌더 스크립트: `assets/analysis/render_gaplc.py` (재실행 가능)
  - 완성 이미지: `assets/image_extraction/japanese_images/GADAT030/translated_png/*.png`
    (76개, 원본과 동일한 208×28 캔버스 크기, 투명 배경 유지)
  - `assets/image_extraction/japanese_images/GADAT030/manifest.json`에 원문/번역/치수 기록
- **미확정 고유명사 주의**: `ゲルン`(게른), `スカイパレス`(스카이팔레스),
  `オ・ケスラ`/`オ・ガウブ`/`アドミラル・レゾム`(적 진영 함선명으로 추정)는
  이 작품 신규 용어라 사용자 확인 없이 표기를 임의로 정했다. 본편 대사 번역
  전에 반드시 용어집으로 확정할 것 — 여기서 정한 표기가 뒤집히면 이 76개
  이미지도 다시 렌더링해야 한다.

## 아직 안 한 이미지 작업

- **GADAT032**(시스템 메뉴, 챕터 제목 13개, CG 갤러리 제목 17개, BGM 목록,
  전투 UI 등 — 1,332개 PNG 중 상당수가 번역 대상으로 추정) — 개수가 훨씬
  많아 이번 세션에서는 손대지 못했다. GADAT030과 같은 방식(1열 직접 대조
  목록 → 번역 데이터 JSON → 렌더 스크립트)으로 이어서 하면 된다.
- **.tex 포맷으로 재인코딩 후 ISO 재삽입**: 지금 나온 건 PNG까지다. 실제
  게임에 넣으려면 1편의 `galaxy_angel_patch_gadat032_images.py` 계열 도구로
  TEX/RAT 포맷 재인코딩 + 컨테이너 재삽입이 필요하다 — 별도 작업.

## 다음 단계 제안

1. **최우선**: `.isb` 포맷 해독을 위한 emucap 런타임 추적 세션을 별도로 진행한다.
   `6100.isb` 같은 특정 파일이 `SCENARIO.DAT`의 어느 압축 블록에 대응하는지부터
   찾고, 그 블록을 대사 화면에서 실행되는 시점에 메모리 스냅샷을 떠서 대조한다.
2. 그동안 이미지 번역(위 카탈로그)과 전투/UI 텍스트(`remaining_candidates.json`)
   번역은 스크립트 해독과 무관하게 먼저 진행할 수 있다.
3. `ルシャーティ`, `ヴァイン`, `ゲルン` 등 신규 고유명사는 용어집 확정 시 함께
   다뤄야 한다.
